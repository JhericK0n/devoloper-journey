# Git Cheats Sheet

## Repository

Git init create internal files from git.

## Status

Git status show the state to the working directory and the staging area.

## Staging 

Git add Add/prepares alteration in the stagin area to the next commit.

## Commit

Registe the Snapshot of prepared changes in the history of local repository.

## Remote

The git fetch search the changes of remote remository and updates references such as origin/mani in the local repository.

The git push sends the commits of local repository from the remote repository.

As for the pull search and make integration on the current branch.

## Branches

Is an independent devolopment line that allows code to be modifiel in isolation.